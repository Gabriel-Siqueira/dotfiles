# -*- mode: snippet -*-
# name: model_for_contests
# key: model
# --
#include <bits/stdc++.h>
using namespace std;

// #define LL
#ifdef LL
	typedef long long num;
#else
	typedef int num;
#endif
typedef complex<num> cpl;
typedef vector<num> vn;
typedef pair<num,num> nn;
typedef pair<nn,num> nnn;
typedef vector<nn> vnn;
typedef map<num,num> mnn;
typedef map<string,num> msn;
typedef set<num> sn;
#define fst first
#define snd second

#define nnn(a,b,c) nnn(nn(a,b),c)
#define ALL(cont) cont.begin(), cont.end()
#define RALL(cont) cont.rbegin(), cont.rend()
#define MEM(v, x) memset(v, x, sizeof(v))
#define SIZE(a) (num)a.size()
#define gcd(a,b) __gcd(a,b)
#define REP(i,n) for(int i = 0; i < n; i++)

#define MOD (1000000000 + 7)
#define PI acos(-1.0)
#ifdef LL
	const long long INF = 0x3f3f3f3f3f3f3f3f;
#else
	const int INF = 0x3f3f3f3f;
#endif
const num MAX = ${1:100100};

template <class ForwardIterator>
void print_vec ( ForwardIterator first, ForwardIterator last ) {
	for(ForwardIterator x = first; x < last; x++) {
		if((int) *x == INF) cout << "INF ";
		else if((int) *x == -INF) cout << "-INF ";
		else cout << *x << " ";
	}
    cout << endl;
}

int main(int argc,char *argv[]){
	ios_base::sync_with_stdio(false);

	num n;

	while(1) {
		cin >> n;
		if(cin.eof()) break;
		$0
	}
	
	return 0;
}
